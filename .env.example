# PostgreSQL connection settings
USER=postgres
PASSWORD=your_password
HOST=localhost
DATABASE=travel_tracker
PORT=5432

# Note: The Express server always listens on port 3000 (configured in index.js).